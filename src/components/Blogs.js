import React from "react"; import {Link} from "react-router-dom"; export default function Blogs({blogs,setBlogs}){if(!blogs.length)return <div className="empty"><h3>No articles found</h3><p>Try another search or category.</p></div>;return <div className="cards">{blogs.map(b=><article className="card" key={b.id}>{b.image&&<img src={b.image} alt={b.title}/>}<div className="card-body"><span className="tag">{b.category}</span><h3>{b.title}</h3><p>{b.content}</p><small>By {b.author||"Admin"} · {b.date}</small><div className="card-actions"><Link to={"/blog/"+b.id}>Read More →</Link>{setBlogs&&<Link to={"/edit/"+b.id}>Edit</Link>}</div></div></article>)}</div>}